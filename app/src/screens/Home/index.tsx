import { JSX } from 'react';
import { SafeAreaView, Text } from 'react-native';

const Home = (): JSX.Element => {
  return (
    <SafeAreaView>
      <Text>Home</Text>
    </SafeAreaView>
  );
};

export default Home;
